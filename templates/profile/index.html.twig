{% extends 'base.html.twig' %}

{% block title %}TheRightCorner - Mon profil{% endblock %}

{% block body %}
    <h1>Mon profil</h1>

    <div class="card">
        <div class="card-header">
            <h4>Pseudo</h4>
        </div>
        <div class="card-body">
            {{ user.username }}
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h4>Prénom/Nom</h4>
        </div>
        <div class="card-body">
            {{ user.firstName }} {{ user.lastName }}
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h4>Email</h4>
        </div>
        <div class="card-body">
            {{ user.email }}
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h4>Mes annonces</h4>
        </div>
        <div class="card-footer">
            <a class="btn btn-primary" href="{{ path('profile_posts') }}">
                Consulter mes annonces
            </a>
        </div>
    </div>
{% endblock %}
